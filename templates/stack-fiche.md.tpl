# Fiche stack technique — ${project_name}

_Établie le ${date} par analyse du code source (lecture seule)._

## Essentiel

| Dimension | Valeur |
| --- | --- |
| Langage back | ${lang_back} |
| Framework back | ${framework_back} |
| Framework front | ${framework_front} |
| Packaging | ${packaging} |
| JVM | ${jvm} |
| Serveur d'application | ${app_server} |
| Base de données | ${database} |

## Détail

- **Langages** : ${languages}
- **Backend / framework** : ${backend}
- **Frontend** : ${frontend}
- **Build & dépendances** : ${build}
- **Persistance** : ${persistence}
- **Déploiement** : ${deployment}
- **Tests** : ${tests}
- **Architecture** : ${architecture}

${notes}
